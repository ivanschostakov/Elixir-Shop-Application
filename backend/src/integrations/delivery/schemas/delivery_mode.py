from typing import Literal

type DeliveryMode = Literal["door", "pickup"]
type CdekDeliveryMode = Literal["pickup", "door", "office"]
type YandexDeliveryMode = Literal["self_pickup", "time_interval"]
