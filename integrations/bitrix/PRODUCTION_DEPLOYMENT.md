# Production deployment: Elixir Bitrix integrations

Этот документ относится к комплекту, подготовленному 28 июля 2026 года. Секреты в Git и ZIP не включаются.

## Состав production

### Обязательно

1. `elixir.promo` — проверка промокодов, расчёт корзины и начислений без записи
   начислений в Bitrix, учёт прогресса покупок и автоматическое создание купона
   из `UF_PROMO`.
2. `elixir.reviewsync` — двусторонний обмен отзывами сайта и приложения.
3. `elixir.delivery` — расчёт доставки приложения через Bitrix Sale и активные настройки IPOL.СДЭК.
4. `elixir.catalogsync` — односторонняя синхронизация описания, применения и хранения из закрытых полей Bitrix в приложение.
5. `app_integration.php` — вход покупателей приложения через учётную запись сайта с последующей локальной email-2FA.
6. `giveaways.php` — совместимый защищённый endpoint действующего giveaway-бота.

AI-чат в этот состав не входит и на сайт не устанавливается.

## Чего в комплекте нет

- старого публичного `createCupon.php`;
- отдельных `get_promo.php` и `sync_promo_coupons.php`;
- постоянной массовой синхронизации купонов;
- прямого доступа приложения к MySQL Bitrix;
- production tokens и паролей;
- фотографий в синхронизации отзывов;
- AI-чата, `ai_tools.php`, компонента `aichat:chat.wrapper` и изменений шаблона сайта.

## Порядок выкладки

### 1. Инвентаризация сервера

До копирования определить:

- фактический `DOCUMENT_ROOT`;
- активный шаблон сайта;
- пользователя и группу PHP-FPM;
- версии PHP и Bitrix;
- наличие `sale`, `catalog`, `iblock`, `sotbit.reviews`, `sotbit.b2c`;
- текущие версии заменяемых файлов;
- путь закрытого каталога вне `public_html`;
- разрешён ли этот путь `open_basedir` каждого production-домена;
- доступность закрытого HTTPS-порта Bitrix с серверов приложения и giveaway.

### 2. Резервная копия

Перед изменением данных сохранить полный дамп базы и затрагиваемые файлы вне
публичного web-root, проверить архив и контрольные суммы. Этот production-бэкап
не считается временным артефактом и после smoke test не удаляется.

### 3. PHP-проверка

На сервере выполнить:

```bash
find <временный_каталог_релиза> -type f -name '*.php' -print0 | xargs -0 -n1 php -l
```

При любой ошибке выкладку остановить.

### 4. Модуль промокодов

Скопировать:

```text
elixir.promo → <DOCUMENT_ROOT>/local/modules/elixir.promo
```

Установить через административную панель. Оставить API и автоматическое создание выключенными. Заполнить настройки и проверить один активный/неактивный код, затем расчёт реальной тестовой корзины.

### 5. Синхронизация отзывов

Скопировать:

```text
elixir.reviewsync → <DOCUMENT_ROOT>/local/modules/elixir.reviewsync
```

Установить модуль, задать отдельный secret, проверить сопоставление `XML_ID` и выполнить разовый обмен при остановленном worker. Периодический worker включать только после сравнения результатов.

### 5.1. Расчёт доставки приложения

Скопировать `elixir.delivery` в `local/modules`, установить и задать отдельный
HMAC secret. Разрешить только IP production backend. Endpoint:

```text
https://elixirpeptide.com:8443/bitrix/tools/elixir.delivery/quote.php
```

Приложение отправляет адрес, а backend сам добавляет фактическую серверную
корзину. Расчёт выполняет активный профиль IPOL.СДЭК; цена, переданная мобильным
клиентом, для CDEK игнорируется. Публичный сайт и его шаблон не изменяются.

### 5.2. Контент карточек приложения

Скопировать:

```text
elixir.catalogsync → <DOCUMENT_ROOT>/local/modules/elixir.catalogsync
```

Модуль создаёт в инфоблоке каталога три app-only HTML-свойства:

- `ELIXIR_APP_DESCRIPTION`;
- `ELIXIR_APP_USAGE`;
- `ELIXIR_APP_STORAGE`.

Они не используются публичным шаблоном сайта. Перед первым включением worker
текущие значения приложения один раз переносятся в пустые свойства Bitrix.
После сверки Bitrix становится единственным источником этих трёх полей.
Название, цена, остаток, варианты, активность и стандартные поля описания сайта
не синхронизируются этим модулем.

### 6. App + Giveaway Bridge

Скопировать только `app_integration.php` и `giveaways.php` из `elixir.sitebridge/local/api`.
Создать отдельные production-конфиги, ограничить каждый endpoint известным IP и
разместить rate-limit каталоги вне `public_html`. AI-файлы не копировать и шаблон
сайта не изменять.

### 7. Backend приложения

Настроить:

```dotenv
BITRIX_PROMO_ENDPOINT=https://elixirpeptide.com:8443/bitrix/tools/elixir.promo/api.php
BITRIX_PROMO_TOKEN=<отдельный_секрет>
BITRIX_PROMO_TIMEOUT_SECONDS=15

BITRIX_DELIVERY_ENDPOINT=https://elixirpeptide.com:8443/bitrix/tools/elixir.delivery/quote.php
BITRIX_DELIVERY_SECRET=<отдельный_секрет>
BITRIX_DELIVERY_TIMEOUT_SECONDS=30

WEBSITE_IDENTITY_ENDPOINT=https://elixirpeptide.com:8443/local/api/app_integration.php
WEBSITE_IDENTITY_TOKEN=<отдельный_секрет>
WEBSITE_IDENTITY_TIMEOUT_SECONDS=15
AUTH_LOGIN_WEBSITE_FIRST_ENABLED=true

WEBSITE_REVIEW_SYNC_ENDPOINT=https://elixirpeptide.com:8443/bitrix/tools/elixir.reviewsync/sync.php
WEBSITE_REVIEW_SYNC_SECRET=<отдельный_секрет>
WEBSITE_REVIEW_SYNC_INTERVAL_MINUTES=1
WEBSITE_REVIEW_SYNC_TIMEOUT_SECONDS=30

WEBSITE_CATALOG_SYNC_ENDPOINT=https://elixirpeptide.com:8443/bitrix/tools/elixir.catalogsync/sync.php
WEBSITE_CATALOG_SYNC_SECRET=<отдельный_секрет>
WEBSITE_CATALOG_SYNC_INTERVAL_MINUTES=5
WEBSITE_CATALOG_SYNC_TIMEOUT_SECONDS=30
```

Покупатель Bitrix сопоставляется по email. Если email не найден на сайте, расчёт
выполняется в анонимном контексте. Администраторы приложения с сайтом не связываются.
Журнал реферальных начислений хранится в PostgreSQL приложения; Bitrix хранит
только прогресс оплаченных покупок, необходимый для общей персональной скидки.

### 8. Закрытый HTTPS-маршрут

Публичные `80/443`, домены, редиректы и шаблон сайта изменять не требуется. Apache
Bitrix уже принимает HTTPS на `8443`. В firewall Hestia разрешить этот порт только
production-IP приложения и giveaway:

```bash
v-add-firewall-rule ACCEPT <APP_IP>/32 8443 TCP ELIXIR_APP_BITRIX_API
v-add-firewall-rule ACCEPT <GIVEAWAY_IP>/32 8443 TCP ELIXIR_GIVEAWAY_BITRIX_API
```

Для всех остальных адресов `8443` должен оставаться закрытым.

### 9. Сопоставление каталога и отзывов

- основной ключ товара — текущий `XML_ID` карточки Bitrix, равный UUID МойСклад;
- сохранённый в старом отзыве `XML_ID_ELEMENT` используется только как резерв;
- UUID варианта приложения также разрешается строго через его родительский товар;
- сопоставления по похожему названию не выполняются;
- готовые товары с текстом «косметическое сырьё» в названии включаются;
- исключается именно папка МойСклад `Сырье и материалы`.

### 10. Smoke test

- выключенный endpoint возвращает `503`;
- запрос без token/подписи возвращает `401`;
- чужой IP возвращает `403`, если allowlist включён;
- активный промокод находится;
- промокод корректно рассчитывается на корзине;
- цена СДЭК рассчитывается Bitrix по реальной корзине и активному профилю IPOL;
- отключённый в Bitrix способ доставки отклоняется;
- повторный расчёт перед созданием заказа не принимает цену мобильного клиента;
- итог совпадает с корзиной сайта Bitrix;
- расчёт начисления не создаёт в Bitrix начисление или бонусную операцию;
- приложение сохраняет начисление один раз и показывает его в админке;
- повторная фиксация покупки не увеличивает прогресс покупателя второй раз;
- несуществующий код отклоняется;
- новый `UF_PROMO` создаёт один купон без дублей;
- отзыв проходит в обе стороны без дублей;
- контент каталога сопоставляется по UUID `XML_ID`, а после пересоздания товара
  в МойСклад — только по уникальному `CML2_ARTICLE`; дубли отклоняются;
- пустые новые поля Bitrix первоначально заполнены текущим контентом приложения;
- повторный цикл контента не создаёт изменений;
- изменение app-only свойства Bitrix появляется в приложении;
- стандартные `PREVIEW_TEXT`, `DETAIL_TEXT` и публичная карточка сайта не изменяются;
- вход существующего покупателя сайта создаёт/обновляет только покупательский профиль приложения и требует email-2FA;
- giveaway-бот получает `404 not_found` для тестового несуществующего email и рабочий ответ для реальной записи;
- серверные логи не содержат secrets или паролей.

## Проверенный результат 28.07.2026

- PHP lint: все PHP-файлы комплекта без синтаксических ошибок;
- backend unit tests: 22/22;
- каталог: 89 товаров и 114 вариантов после штатной синхронизации МойСклад;
- отзывы Bitrix → приложение: 88/88, пропущено 0;
- повторный цикл: изменений 0, конфликтов 0;
- в БД приложения: 88 уникальных remote ID, дублей 0;
- приложение → Bitrix: тестовый отзыв создан и получил remote ID, затем обе тестовые записи удалены;
- identity: вход временного покупателя сайта прошёл;
- promo: lookup и расчёт реальной корзины прошли, скидка положительная;
- giveaway: `get_user_id` и `find_review` прошли;
- все временные smoke-пользователи, купоны и отзывы удалены;
- публичное поведение сайта не менялось.

## Откат

1. Выключить новые endpoint и workers.
2. Вернуть backend на предыдущую конфигурацию.
3. Вернуть прежние PHP-файлы.
4. Очистить кеш Bitrix/OPcache.
5. Проверить сайт, корзину и авторизацию.

Удаление модулей не должно удалять отзывы или уже созданные купоны.
